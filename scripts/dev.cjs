// Temporary no-op dev script for local development
// The original project may include environment setup for cloudflare or remixed configs.
// This stub ensures `npm run dev` can proceed to `remix vite:dev` in the local copy.

console.log('scripts/dev.cjs: no-op script — continuing to remix vite:dev');
process.exit(0);
